import { CasesPageSkeleton } from "../page-skeleton"

export default function CasesSortPage() {
  return (
    <CasesPageSkeleton
      title="案例排序"
      description="調整案例在前台案例櫥窗中的顯示順序。"
      placeholder="這裡將可透過拖曳調整案例在前台案例櫥窗中的顯示順序。"
    />
  )
}
