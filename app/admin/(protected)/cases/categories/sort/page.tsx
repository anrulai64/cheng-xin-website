import { CasesPageSkeleton } from "../../page-skeleton"

export default function CaseCategoriesSortPage() {
  return (
    <CasesPageSkeleton
      title="分類排序"
      description="調整案例分類在前台的顯示順序。"
      placeholder="這裡將可透過拖曳調整分類在前台的顯示順序。"
    />
  )
}
